# Infrastructure placeholder

This directory is reserved for future infrastructure-as-code. The selected
Azure topology and identity model are documented in the accepted ADRs, but no
deployment resources are implemented here yet. In particular, issue #37 adds
application authentication seams without provisioning Entra app registrations,
federated credentials, managed identities, or role assignments.
