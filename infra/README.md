# Infrastructure placeholder

This directory is reserved for future infrastructure-as-code. Azure is a
possible deployment target, but no provider, resource topology, network model,
identity model, or deployment workflow has been decided.
